document.addEventListener('DOMContentLoaded', function() {
  // Submit Composed Email
  document.querySelector('#post-form').addEventListener('submit', send_post);
  // By default, load the inbox
  load_post();
});

function load_post(){
  
}